- deploy the db to mlab? and deploy the server to heroku - can't do that because heroku is a stinker. so find an alternative
- refactor the findOne method in the controller to be re-usable is it?
- linter maybe
- typescript maybe